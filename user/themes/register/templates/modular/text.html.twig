<section class="content-wrapper {{ page.header.body_classes }}">
    <div class="container">
        <div class="page-header">
            <div class="subtitle">{{ page.header.subtitle }}</div>
            <h1>{{ page.title }}</h1>
        </div>
        <div class="modular-row callout">
            {{ content }}
        </div>
    </div>
</section>